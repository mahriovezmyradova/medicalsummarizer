streamlit>=1.31.0
pandas>=2.0.0
fpdf2>=2.7.7
python-dateutil>=2.8.2

reportlab>=4.0.0
Pillow>=10.0.0
openai>=1.10.0

# Lightweight runtime deps
numpy>=1.24.0
requests>=2.31.0
boto3>=1.26.0

# Heavy local models — only install when running locally, NOT on Streamlit Cloud
# openai-whisper>=20231117   <- pulls PyTorch (~2GB RAM)
# sentence-transformers>=2.2.2  <- pulls PyTorch (~500MB RAM)
# scikit-learn>=1.2.2
